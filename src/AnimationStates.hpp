#ifndef ANIMATION_STATES_HPP
# define ANIMATION_STATES_HPP

enum PlayerState {
	NOTHING,
	IDLE,
	UP,
	DOWN,
	LEFT,
	RIGHT
};

enum Keybindings {
	MOVE_UP,
	MOVE_RIGHT,
	MOVE_DOWN,
	MOVE_LEFT
};

#endif
